const navToggle=document.querySelector(".nav-toggle"),nav=document.querySelector(".main-nav");navToggle.addEventListener("click",()=>{const open=nav.classList.toggle("open");navToggle.setAttribute("aria-expanded",String(open))});nav.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{nav.classList.remove("open");navToggle.setAttribute("aria-expanded","false")}));const slider=document.querySelector("#fuel-spend"),spendLabel=document.querySelector("#spend-label"),pointsResult=document.querySelector("#points-result");function updatePoints(){const amount=Number(slider.value);spendLabel.textContent=new Intl.NumberFormat("en-PH",{style:"currency",currency:"PHP",maximumFractionDigits:0}).format(amount);pointsResult.textContent=Math.floor(amount/100)}slider.addEventListener("input",updatePoints);updatePoints();document.querySelectorAll("[data-station]").forEach(btn=>btn.addEventListener("click",()=>{const station=btn.dataset.station;window.open("https://www.google.com/maps/search/?api=1&query="+encodeURIComponent("EcoOil "+station+" Cagayan de Oro"),"_blank","noopener")}));function demoSubmit(form,message){form.addEventListener("submit",event=>{event.preventDefault();const status=form.querySelector(".form-status");status.textContent=message;form.querySelector("button").textContent="Received ✓";form.querySelector("button").disabled=true})}demoSubmit(document.querySelector("#rewards-form"),"Demo completed. In the live system, the customer would receive a membership confirmation and QR card.");demoSubmit(document.querySelector("#business-form"),"Demo inquiry received. In the live system, this would be assigned to a fleet representative.");const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add("visible")}),{threshold:.12});document.querySelectorAll(".section-heading,.steps,.points-demo,.promo-grid,.station-grid,.fleet-copy,.form-intro,.lead-form,.business-form").forEach(el=>{el.classList.add("reveal");observer.observe(el)});