const navToggle=document.querySelector(".nav-toggle"),nav=document.querySelector(".main-nav");navToggle.addEventListener("click",()=>{const open=nav.classList.toggle("open");navToggle.setAttribute("aria-expanded",String(open))});nav.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{nav.classList.remove("open");navToggle.setAttribute("aria-expanded","false")}));const form=document.querySelector("#order-form");form.addEventListener("submit",event=>{event.preventDefault();const status=form.querySelector(".form-status"),button=form.querySelector("button");status.textContent="Demo request completed. In the live site, station management would receive this enquiry for confirmation.";button.textContent="Request prepared ✓";button.disabled=true});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add("visible")}),{threshold:.1});document.querySelectorAll(".section-heading,.service-grid,.delivery-copy,.store-layout,.rewards-banner>div,.location-copy,.order-intro,.order-form").forEach(el=>{el.classList.add("reveal");observer.observe(el)});